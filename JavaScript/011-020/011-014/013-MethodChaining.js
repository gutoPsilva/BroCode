// method chaining = chamar um método atrás do outro 
//                   numa linha contínua de código

let userName = "gustavo";

//let letter = userName.charAt(0);
//letter = letter.toUpperCase();

let letter = userName.charAt(0).toUpperCase(); // faz o mesmo que as 2 linhas acima, numa só. É ISSO, CABÔ POH@.

console.log(letter);