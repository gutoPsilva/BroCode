let students = 20;

//students = students + 1;
//students = students - 1;
//students = students * 2;
//students = students / 2;
//let extraStudents = students % 2;


// Shorthands que diminuem o tamanho acima, e fazem o mesmo aqui embaixo. Não há isso para o módulo (%)
//students += 1;
//students -= 1;
//students *= 2;
//students /= 2;

/*
    Precedência de operadores é respeitada
    1. parenteses ()
    2. expoentes
    3. multiplicação & divisão
    4. adição e subtração
*/

let result = (1+2) * (4+4); // 3 * 8 = 24
console.log(result)